const passwordErrorMessage =
  'Required at least 6 characters , one upper case letter, and one number.';

const phoneNumberErrorMessage = 'Required at least 12 digits.';

const credentialsErrorMessage = 'Required only letters.';

const personalDataErrorMessage = 'Required at least 10 digits';

export {
  passwordErrorMessage,
  phoneNumberErrorMessage,
  personalDataErrorMessage,
  credentialsErrorMessage,
};
